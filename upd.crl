#!/bin/bash

SCRIPTPATH="$( cd -- "$(dirname "$0")" >/dev/null 2>&1 ; pwd -P )" #"
. $SCRIPTPATH/_config

openssl ca -gencrl -keyfile $ovpndir/ca.key -cert $ovpndir/ca.pem -crldays 720 -out $ovpndir/clients/revoked.crl -config $sslconf

